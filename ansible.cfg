[defaults]
inventory = inventory           # Specifies the path to the inventory file or directory
roles_path = roles             # Directory where Ansible roles are stored
retry_files_enabled = False    # Disable creation of retry files after failed playbook runs
